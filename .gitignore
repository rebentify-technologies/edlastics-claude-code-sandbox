.DS_Store
.env

# Team canonical clones and developer IDE checkouts — populated by /clone-repos
/dev-agent/
/dev-local/

# Sandbox repo's own ephemeral worktrees for PR work
/worktrees/wt-*

# pnpm content-addressable store (shared across all repos in the container)
/.pnpm-store/
